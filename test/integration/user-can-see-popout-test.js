describe("User can see popout", () => {
  beforeAll(async () => {
    await page.goto("http://localhost:8080");
  });

  it("and see the first image", () => {

  });

  it("and see the same number of circles as there are images", () => {

  });

  it("and see the first circle highlighted", () => {

  });

  it("and see arrows for left and right", () => {

  });
})
